// project import
import authentication from './authentication';
import pages from './pages';

// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
  items: [pages, authentication]
};

export default menuItems;
