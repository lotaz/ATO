import { Metadata } from "next";

export const metadata: Metadata = {
  title: " Page ",
  description: "This is a Page ",
  // other metadata
};

const Page = () => {
  return;
  <></>;
};

export default Page;
